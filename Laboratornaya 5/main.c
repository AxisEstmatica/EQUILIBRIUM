#pragma warning(disable : 4996) // отключение ошибки c4996
#include <stdio.h>

#define N 10

int main(void)
{
  char MAS[N];
  
  for (i = 0; i < N; ++i)
  {
    printf("Number %d, i+1") // ne znaiy kak budet rabotat'
    scanf("%d", &MAS[i]);
  }
  
return 0;
}
